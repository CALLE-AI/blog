# CALL-E Blog 通用制作与维护流程

这份文档用于以后新增、修改和发布 CALL-E 博客。当前站点是静态网站，代码仓库为 `CALLE-AI/blog`，GitHub Pages 从 `main` 分支的 `docs/` 目录发布，正式域名为 <https://blog.heycall-e.com/>。

## 1. 开始前收集资料

每篇文章至少准备以下内容：

- 原文：唯一的正文内容依据，最好是 Markdown、纯文本或可复制的文档。
- 标题、作者、分类、发布日期和摘要。
- 文章 URL slug，例如 `ai-can-make-calls-but-can-it-navigate-an-ivr-system`。
- 文章涉及的原始图片、图表和可核验的数据来源。
- 视觉参考：参考其排版、节奏和图片表达，不复制对方品牌资产或文案。

资料的优先级如下：

1. 已确认的文章原文和作者修改意见。
2. CALL-E 官网正在使用的品牌资源与公开信息。
3. CALL-E 既有博客的视觉规范。
4. Sierra 等外部技术博客，只作为设计参考。
5. 截图、聊天记录和 ZIP 示例，用于理解需求，不自动视为正文。

开始编辑前，应把“必须照做的内容要求”和“仅供参考的材料”分开。正文没有的信息不要擅自补充；需要新增说明时，先让作者确认。

## 2. 内容编辑与校对

### 保持原文

- 标题、段落顺序、事实、数字、专有名词和结论以原文为准。
- 不为了版式把普通段落改成卡片、标语、流程图或额外小标题。
- 原文是列表时使用真正的 `<ul>` / `<ol>`，显示 bullet 或编号。
- 原文是自然段时保持自然段，不拆成列表。
- 引用、示例和强调只在原文存在相应语义时使用。
- 外链、作者名、日期、产品名、大小写和标点逐项核对。

### 建议采用两轮校对

第一轮对内容：逐段比较 HTML 与原文，检查遗漏、改写、重复和顺序变化。

第二轮对页面：在浏览器中阅读完整文章，检查标题层级、换行、列表、图片、链接和移动端显示。不要只检查源代码。

## 3. 页面与视觉规范

### 品牌资源

- Logo、图标、字体和品牌色优先从 CALL-E 官网取得正式资源。
- 当前 Logo 文件为 `docs/logo-call-e.svg`，不要用字母圆圈或手绘近似图替代。
- 正文内容优先使用统一的正文字体；装饰字体只在品牌明确需要时使用。
- 修改全站字体、Logo 或颜色时，同时检查首页、文章页、页脚和移动端。

### 排版原则

- 标题层级必须一眼可辨：页面标题、章节标题和小标题应有稳定且不同的字号与间距。
- 正文以阅读为主，少用无来源的色块、边框、标签、超大引语和装饰性组件。
- 段落宽度与行距保持适合长文阅读；不要让图片或图表压过正文。
- 文章目录只收录真实章节标题，不额外发明标题。
- 首页卡片可参考 Sierra 的“标题 + 摘要 + 关键词视觉图”结构，但保持 CALL-E 自己的 Logo、颜色和语气。

### 文章配图

- 首页封面图提取文章的一个核心关键词，不在图片中重复完整标题。
- 同一批封面图使用一致的比例、圆角、文字位置、颗粒感和色彩逻辑。
- 图片文件名使用小写英文和连字符，例如 `blog-ivr-navigation.png`。
- 每张图片提供准确的 `alt` 文本；装饰图不要写成正文内容。
- 社交分享图使用 `docs/og.png`，推荐尺寸为 1200 × 630，并检查标题在常见裁切区域内仍可读。
- 技术图表应控制显示宽度，同时允许打开原图查看细节。

## 4. 文件结构与新增文章

当前发布目录结构如下：

```text
docs/
├── index.html
├── CNAME
├── .nojekyll
├── logo-call-e.svg
├── og.png
├── <article-slug>/
│   └── index.html
└── _next/static/...
```

新增文章时：

1. 确定唯一且稳定的英文 slug。
2. 在 `docs/<article-slug>/index.html` 创建文章页。
3. 在 `docs/index.html` 添加文章入口。
4. 添加封面图及文章内图片，并使用以 `/` 开头的根路径，例如 `/blog-ivr-navigation.png`。
5. 设置页面的 `title`、description、canonical URL、Open Graph 和 Twitter 分享信息。
6. 首页文章入口使用站内相对路径；正式发布前检查没有遗留 localhost 或临时预览链接。

除非明确要更换域名，不要修改 `docs/CNAME`。当前内容应为：

```text
blog.heycall-e.com
```

## 5. 本地预览与验收

在仓库根目录运行：

```bash
python3 -m http.server 8000 -d docs
```

打开 <http://localhost:8000/>，至少检查：

- 首页能打开，所有文章入口可点击。
- 文章页、Logo、封面图、正文图片和 CSS 均返回成功。
- 页面中没有图片破损、裸 HTML、无样式内容或横向溢出。
- 桌面端和窄屏手机端都可阅读。
- 标题层级、段落、列表与原文一致。
- 站内链接、官网链接、文档链接和社群链接目标正确。
- 浏览器控制台没有资源 404。
- `git diff --check` 通过，且提交中没有密钥、`.env` 或无关文件。

可用以下命令做基础检查：

```bash
curl -fsS http://localhost:8000/ >/dev/null
curl -fsS http://localhost:8000/<article-slug>/ >/dev/null
git diff --check
git status --short
```

## 6. 提交与上传

先确认改动范围：

```bash
git diff --stat
git diff
```

提交并推送：

```bash
git add <changed-files>
git commit -m "feat(blog): publish <short-article-name>"
git push origin main
```

常用提交类型：

- 新文章：`feat(blog): publish ...`
- 内容或样式修正：`fix(blog): ...`
- 资源或维护工作：`chore(blog): ...`

当前 GitHub Pages 直接发布 `main/docs`，推送到 `main` 会触发公开网站更新。因此推送前必须完成本地校对，不要把半成品、内部资料或未确认文案提交到仓库。

## 7. 发布后验证

在 GitHub 仓库的 Actions 页面确认 `pages build and deployment` 成功，也可以运行：

```bash
gh run list --repo CALLE-AI/blog --limit 5
```

随后检查：

- 首页：<https://blog.heycall-e.com/>
- 文章：`https://blog.heycall-e.com/<article-slug>/`
- Logo、CSS、封面图和正文图片可以直接访问。
- 页面源代码中的 canonical 和分享图 URL 已指向正式域名。
- 从聊天工具或社交平台粘贴链接时，标题、描述和 `og.png` 显示正确。

GitHub Pages 和浏览器可能缓存 HTML、CSS 与图片。若代码已部署但页面仍显示旧版：

1. 先确认 GitHub Pages Action 已成功完成。
2. 使用无痕窗口或强制刷新（macOS：`Cmd + Shift + R`）。
3. 给页面 URL 临时增加查询参数，例如 `?v=<commit>`，判断是否为缓存。
4. 修改 CSS 或图片后，如需立即强制更新，可在 HTML 的资源地址后增加版本参数，例如 `style.css?v=4`。
5. 不要仅凭本地文件判断上线成功，必须从正式域名重新请求并验证。

## 8. 后续维护

### 每次新增文章

- 更新首页文章列表和排序。
- 更新文章页元数据及分享图。
- 保持图片命名和卡片比例一致。
- 检查新文章和已有文章在桌面、移动端均未被破坏。

### 修改公共样式或品牌资源

- 先检查首页和至少一篇长文章。
- 同时检查 header、footer、列表、正文和响应式断点。
- 必要时更新资源版本，避免用户继续读取旧缓存。

### 修改外链或域名

- 全仓搜索旧地址，检查 HTML、canonical、Open Graph、CNAME 和 README。
- DNS 或 GitHub Pages 自定义域名发生变化时，记录变更人和生效时间。
- 不删除仍可能被旧页面引用的资源，除非确认缓存和引用已经失效。

### 定期检查

建议每季度检查一次：

- 外部文章链接是否仍有效。
- 官网 Logo、品牌色和字体是否已更新。
- GitHub Pages 构建是否稳定。
- 自定义域名和 HTTPS 是否正常。
- 首页摘要、作者信息和文章排序是否需要更新。

## 9. 发布检查清单

```text
[ ] 原文与 HTML 逐段核对完成
[ ] 标题、作者、分类、日期、摘要和 slug 已确认
[ ] 没有添加原文不存在的事实或文案
[ ] 列表与自然段结构正确
[ ] Logo、字体、图片和 alt 文本正确
[ ] 首页入口和文章页链接正确
[ ] canonical、OG 和 Twitter 元数据正确
[ ] 桌面端和移动端预览通过
[ ] 本地无 404、无横向溢出、无控制台资源错误
[ ] git diff 仅包含本次改动
[ ] GitHub Pages 部署成功
[ ] 正式域名和分享预览验证完成
```

